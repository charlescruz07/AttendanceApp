<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.acer.attendanceapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/clock"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize">

        <meta-data android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>

        <activity
            android:name=".LoginSignup.LoginActivity"
            android:theme="@style/MyStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginSignup.RolePickActivity"
            android:theme="@style/MyStyle"/>
        <activity android:name=".TeacherSide.TeacherActivity"
            android:theme="@style/MyStyle"/>
        <activity android:name=".StudentSide.StudentActivity"/>
        <activity android:name=".TeacherSide.CreateClassActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/MyStyle"
            />
        <activity android:name=".TeacherSide.ClassDetailsActivity"
            android:theme="@style/MyStyle"/>
    </application>

</manifest>